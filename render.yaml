services:
  - type: web
    name: velixa-api
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:server
    startCommand: npm run start:server
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
